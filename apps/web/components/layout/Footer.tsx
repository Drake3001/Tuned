export function Footer() {
  return (
    <footer className="border-t border-border/60 py-6 text-center text-xs text-muted-foreground">
      tuned.gg — color matching · {new Date().getFullYear()}
    </footer>
  );
}
