export function HeroSlogan() {
  return (
    <section className="mx-auto max-w-4xl px-6 pb-12 pt-24 text-center">
      <h1 className="text-6xl font-bold tracking-tight md:text-8xl">
        match the
        <span className="ml-3 bg-gradient-to-r from-red-500 via-yellow-400 to-blue-500 bg-clip-text text-transparent">
          color
        </span>
      </h1>
      <p className="mt-6 text-lg text-muted-foreground">
        train your eye. one color shown, one color picked, repeat ×5.
      </p>
    </section>
  );
}
